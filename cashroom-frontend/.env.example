# cashroom-frontend — Vite env (must be prefixed VITE_ to reach the browser).
# The browser talks ONLY to the BFF. Host dev + compose both use the host-mapped
# port, because the browser runs on your machine, not inside the compose network.
VITE_BFF_URL=http://localhost:3001
